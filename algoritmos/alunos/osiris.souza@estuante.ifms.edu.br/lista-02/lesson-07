#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>
#include <math.h>

// função principal
int main()
{

  float const salario = 1212.00, desconto = 0.10, multa = 0.15;
  float valorWatts = 0.60, valorFatura, w;
  float a, b, c, d, e;

  printf("Digite o w usado: ");
  scanf("%f", &w);

  a = valorWatts / 100; // valor de cada w

  b = valorWatts * w;

}